name: dependabot

on:
    schedule:
        - cron: '0 0 * * *'
    workflow_dispatch:

jobs:
    dependabot:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4